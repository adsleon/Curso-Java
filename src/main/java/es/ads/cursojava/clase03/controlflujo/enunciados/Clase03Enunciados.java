/*
 * Copyright (c) 2025 Rubén Bécares Álvarez
 *
 * Licencia: CC BY-NC 4.0
 * Uso permitido solo para fines personales o educativos.
 * Prohibido el uso comercial sin autorización expresa del autor.
 * Última modificación: 14/10/2025
 *
 *
 */

package es.ads.cursojava.clase03.controlflujo.enunciados;

public class Clase03Enunciados {

    // ================================
    // A. Condicionales simples (if / else)
    // ================================

    // 1. Usa un if para devolver "Positivo" si n mayor que 0, o "Negativo" si n menor que 0
    public String ejercicio1(int n) {
        return "";
    }

    // 2. Usa if/else para devolver "Par" si el número es par, "Impar" si no lo es
    public String ejercicio2(int n) {
        return "";
    }

    // 3. Usa if/else para devolver "Mayor de edad" si edad mayor o igual que 18, "Menor de edad" si no
    public String ejercicio3(int edad) {
        return "";
    }

    // 4. Usa if/else para devolver el mayor entre dos números
    public int ejercicio4(int a, int b) {
        return 0;
    }

    // 5. Usa if/else para devolver el menor entre dos números
    public int ejercicio5(int a, int b) {
        return 0;
    }

    // 6. Usa if/else para devolver "Aprobado" si nota mayor o igual que 5, "Suspenso" si no
    public String ejercicio6(double nota) {
        return "";
    }

    // 7. Usa if/else para devolver "Positivo", "Negativo" o "Cero"
    public String ejercicio7(int n) {
        return "";
    }

    // 8. Usa if/else para devolver "Múltiplo de 3" o "No múltiplo de 3"
    public String ejercicio8(int n) {
        return "";
    }

    // 9. Usa if/else para verificar si una persona puede conducir (edad mayor o igual que 18)
    public boolean ejercicio9(int edad) {
        return false;
    }

    // 10. Usa if/else para devolver "Número dentro de rango" si 1 menor o igual que n y n menor o igual que 10
    // si no devuelve "Número fuera de rango"
    public String ejercicio10(int n) {
        return "";
    }

    // ================================
    // B. Condicionales compuestos (if / else if / else)
    // ================================

    // 11. Usa if/else if/else para devolver la calificación textual:
    // nota mayor o igual que 9 -> "Sobresaliente", mayor o igual que 7 -> "Notable", mayor o igual que 5 -> "Aprobado", <5 -> "Suspenso"
    public String ejercicio11(double nota) {
        return "";
    }

    // 12. Usa if/else para devolver el mayor de tres números
    public int ejercicio12(int a, int b, int c) {
        return 0;
    }

    // 13. Usa if/else para clasificar un número:
    // menor que 0 -> "Negativo", 0 -> "Cero", mayor que 0 -> "Positivo"
    public String ejercicio13(int n) {
        return "";
    }

    // 14. Usa if/else para clasificar edad:
    // menor que 13 -> "Niño", menor que 18 -> "Adolescente", mayor o igual que 18 -> "Adulto"
    public String ejercicio14(int edad) {
        return "";
    }

    // 15. Usa if/else para devolver "Divisible entre 2 y 3" si cumple ambas condiciones
    // si no "No divisible entre 2 y 3"
    public String ejercicio15(int n) {
        return "";
    }

    // 16. Usa if/else para verificar si un año es bisiesto (buscar como se calcula)
    public boolean ejercicio16(int anio) {
        return false;
    }

    // 17. Usa if/else para devolver el signo zodiacal según un mes (simplificado)
    // enero -> Capricornio, febrero -> Acuario, marzo -> Piscis ...
    public String ejercicio17(int mes) {
        return "";
    }

    // 18. Usa if/else para verificar si una letra es vocal o consonante
    public String ejercicio18(char letra) {
        return "";
    }

    // 19. Usa if/else para verificar si una contraseña coincide con "admin123"
    public boolean ejercicio19(String password) {
        return false;
    }

    // 20. Usa if/else para devolver "Dentro del rango" si el número está entre 100 y 200
    // si no "Fuera del rango"
    public String ejercicio20(int n) {
        return "";
    }

    // ================================
    // C. Switch
    // ================================

    // 21. Usa switch para devolver el nombre del día (1=Lunes, 2=Martes, etc.)
    public String ejercicio21(int dia) {
        return "";
    }

    // 22. Usa switch para devolver el nombre del mes (1=Enero, 2=Febrero, etc.)
    public String ejercicio22(int mes) {
        return "";
    }

    // 23. Usa switch para devolver el tipo de figura:
    // 1->"Círculo", 2->"Cuadrado", 3->"Triángulo"
    public String ejercicio23(int tipo) {
        return "";
    }

    // 24. Usa switch para devolver "Vocal" o "Consonante" según el carácter
    public String ejercicio24(char letra) {
        return "";
    }

    // 25. Usa switch para devolver "Aprobado" si calificación es 'A', 'B', 'C'; "Suspenso" si 'D' o 'F'
    public String ejercicio25(char calificacion) {
        return "";
    }

    // ================================
    // D. Bucles for
    // ================================

    // 26. Usa un for para sumar los números del 1 al 10
    public int ejercicio26() {
        return 0;
    }

    // 27. Usa un for para sumar los números pares del 1 al 10
    public int ejercicio27() {
        return 0;
    }

    // 28. Usa un for para contar cuántos múltiplos de 5 hay del 1 al 50
    public int ejercicio28() {
        return 0;
    }

    // 29. Usa un for para calcular el factorial de un número
    public int ejercicio29(int n) {
        return 0;
    }

    // 30. Usa un for para concatenar los números del 1 al 5 en un String "12345"
    public String ejercicio30() {
        return "";
    }

    // 31. Usa un for para sumar los números impares hasta n
    public int ejercicio31(int n) {
        return 0;
    }

    // 32. Usa un for para contar las vocales en un texto
    public int ejercicio32(String texto) {
        return 0;
    }

    // 33. Usa un for para calcular la potencia base^exponente (ej: 2^3=8)
    public int ejercicio33(int base, int exponente) {
        return 0;
    }

    // 34. Usa un for para generar la tabla de multiplicar de un número y devolver la suma de los productos
    public int ejercicio34(int n) {
        return 0;
    }

    // 35. Usa un for para sumar los números del 1 al n que sean divisibles entre 3 o 5
    public int ejercicio35(int n) {
        return 0;
    }

    // ================================
    // E. Bucles while
    // ================================

    // 36. Usa while para sumar del 1 al 5
    public int ejercicio36() {
        return 0;
    }

    // 37. Usa while para contar desde 10 hasta 1
    public String ejercicio37() {
        return "";
    }

    // 38. Usa while para calcular la suma de los dígitos de un número
    public int ejercicio38(int n) {
        return 0;
    }

    // 39. Usa while para invertir los dígitos de un número
    public int ejercicio39(int n) {
        return 0;
    }

    // 40. Usa while para multiplicar todos los números pares hasta n
    public int ejercicio40(int n) {
        return 0;
    }

    // ================================
    // F. Bucles do-while
    // ================================

    // 41. Usa do-while para sumar los números hasta que la suma supere 50
    public int ejercicio41() {
        return 0;
    }

    // 42. Usa do-while para generar una cadena con los números del 1 al 5
    public String ejercicio42() {
        return "";
    }

    // 43. Usa do-while para calcular el número de dígitos de un entero
    public int ejercicio43(int n) {
        return 0;
    }

    // 44. Usa do-while para contar cuántos múltiplos de 7 hay hasta 100
    public int ejercicio44() {
        return 0;
    }

    // 45. Usa do-while para encontrar el primer número divisible entre 9 y 11
    public int ejercicio45() {
        return 0;
    }

    // ================================
    // G. Control con break y continue
    // ================================

    // 46. Usa un for y break para detener el bucle al llegar a 5 y devolver la suma acumulada
    public int ejercicio46() {
        return 0;
    }

    // 47. Usa un for y continue para sumar solo los números pares del 1 al 10
    public int ejercicio47() {
        return 0;
    }

    // 48. Usa un bucle para encontrar el primer múltiplo de 13 entre 1 y 100
    public int ejercicio48() {
        return 0;
    }

    // 49. Usa un bucle para contar los números entre 1 y 20 que no son múltiplos de 3
    public int ejercicio49() {
        return 0;
    }

    // 50. Usa un bucle con break para detenerse cuando la suma supere 100 y devolver el último número sumado
    public int ejercicio50() {
        return 0;
    }
}
